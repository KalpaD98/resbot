version: "3.1" # rules-restaurant-booking-cancel.yml
rules:

  - rule: User wants to cancel a booking (logged in)
    condition:
      - slot_was_set:
          - is_authenticated: true
    steps:
      - intent: user_wants_to_cancel_booking
      - action: action_show_future_bookings_carousal